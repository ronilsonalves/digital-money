apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: default
  labels:
    app: accounts
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: accounts
  type: ClusterIP